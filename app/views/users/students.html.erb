<h1>STUDENTS</h1>
<h3>lists students currently attending</h3>
<h5>file located users/students.html.erb</h5>

<% if @user %>
	<ul>
		<% @user.reverse.each do |student| %>
			<li>
			<%= student.fname %>
			<%= student.lname %>
			<h3> must be fixed, CURRENTLY LISTING ALL USERS </h3>
			</li>
		<% end %>
	</ul>
<% end %>